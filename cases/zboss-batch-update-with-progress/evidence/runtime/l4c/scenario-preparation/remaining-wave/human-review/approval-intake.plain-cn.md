# SH-3C 极简确认单

你不需要理解哈希、Seed、collector、binding。

这一步只是在确认一件事：是否允许 migration-guard 在一个短时间窗口里，对指定测试环境做一次可清理的真实演练。

## 你只需要回答 5 个问题

### 1. 谁确认这次演练？

- 确认人：
- 工单或记录编号：

### 2. 允许从什么时候跑到什么时候？

- 开始时间：
- 结束时间：

结束时间必须在未来，并且距离开始时间不超过 24 小时。

### 3. 允许碰哪个数据库范围？

- 数据库地址：
- 数据库名：
- 表名：
- 用来标记测试数据的字段或前缀：

意思是：只允许写带这个标记的测试数据，跑完必须清理干净。

### 4. Redis 和 WebSocket 怎么处理？

- Redis 是否参与：参与 / 不参与
- 如果参与，Redis 地址和 key 前缀：
- WebSocket 地址：

如果你不知道 Redis 是否参与，可以写“不确定”，不要猜。

### 5. 故障演练开关在哪里？

下面 4 个场景需要故障开关地址。如果当前环境没有这些开关，就直接写“没有”。

- 提交后副作用失败：
- 表结构/DDL 切换失败：
- 事务失败：
- undo 投递失败：

## 可以直接这样回复

```text
确认人：
工单：
授权时间：

数据库范围：
Redis：
WebSocket：

4 个故障开关：

我确认：允许在上述时间窗口和资源范围内做这次 disposable 测试演练，跑完必须清理。
```

## 如果你无法确认

回复下面任意一句就行：

- 我只能确认场景逻辑，不能授权真实写入。
- 我不知道测试环境资源范围。
- 当前没有故障开关地址。
- 先不要跑真实演练，只继续做静态准备。
